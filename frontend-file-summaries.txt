{
  "frontend/src/App.tsx": "Root app component managing auth state, call state machine, tab navigation, splash screen, and error toast; fixes type guards for `userProfile` (undefined vs null), passes correct props to RegistrationPage/PairingPage, and uses correct `onCallActive` prop signature for incoming call overlays.",
  "frontend/src/hooks/useQueries.ts": "Centralized React Query hooks for all backend operations; updated `useCreateCallSession` to accept `{ receiverId, callType }` with `CallType` enum support (defaults to `CallType.voice`), and exports `CallType` for use in call pages.",
  "frontend/src/pages/LandingPage.tsx": "Full-screen romantic landing page with the generated background image, blush overlay gradient, OnlyUs branding with serif font, Internet Identity sign-in button, and footer attribution.",
  "frontend/src/pages/RegistrationPage.tsx": "Username registration form page with a centered card, romantic styling, input validation, error handling for the 2-user limit, and a logout option.",
  "frontend/src/pages/PairingPage.tsx": "Pairing screen that shows the user's 6-digit couple code (with copy button) if they are User 1, or an OTP input field and \"Pair Up\" button if they are User 2, with error handling and decorative background hearts.",
  "frontend/src/pages/ChatPage.tsx": "Chat screen refactored to fill available height within the tab navigator layout (removed full-screen min-h-screen, removed logout button and footer since those moved to App.tsx global header).",
  "frontend/src/utils/formatTimestamp.ts": "Utility that converts Motoko nanosecond timestamps (as strings) to human-readable \"Today HH:MM\", \"Yesterday HH:MM\", or \"Mon D HH:MM\" format.",
  "frontend/src/index.css": "Global styles with OKLCH romantic color system (blush white, rose pink, warm accent), Playfair Display serif + Inter sans-serif fonts imported from Google Fonts, and custom shadow utilities.",
  "frontend/tailwind.config.js": "Extended Tailwind config with the full romantic color palette tokens (blush-white, rose-pink, warm-accent, rose-dark), Playfair Display/Inter font families, extra border-radius values, and custom romantic shadow definitions.",
  "frontend/src/components/BottomTabNavigator.tsx": "Fixed bottom tab bar with 4 tabs: Chat, Camera, Gallery, and the new Calls tab (Phone icon); active tab highlighted with warm-accent, inactive tabs use muted rose, consistent with existing design.",
  "frontend/src/components/PhotoModal.tsx": "Full-screen photo modal with pinch-to-zoom (touch) and mouse-wheel zoom support, drag-to-pan when zoomed, close button, zoom controls, and uploader/timestamp metadata overlay.",
  "frontend/src/pages/CameraPage.tsx": "Camera capture page with live viewfinder, canvas-based image compression, and gallery upload; fixes the `Uint8Array<ArrayBuffer>` type error by using `.buffer.slice()` to produce a guaranteed plain `ArrayBuffer` before constructing the typed array passed to `ExternalBlob.fromBytes`.",
  "frontend/src/pages/GalleryPage.tsx": "Gallery page with paginated 3-column grid, lazy-loaded images via `ExternalBlob.getDirectURL()`, skeleton loading, empty state, delete confirmation, and full-screen photo modal using the existing `PhotoModal` interface (`imageUrl`/`isOpen`/`onClose` props).",
  "frontend/src/pages/CallsPage.tsx": "Calls tab page showing voice/video call buttons and call history list; uses proper ES module imports for useCreateCallSession, displays skeleton loading states, empty state illustration, and call history with type/status icons.",
  "frontend/src/pages/OutgoingCallPage.tsx": "Full-screen outgoing voice call overlay with WebRTC offer creation, SDP storage, session polling, and cancel logic; updated `createSession.mutateAsync` call to pass `{ receiverId: partnerId, callType: CallType.voice }` matching the new mutation signature.",
  "frontend/src/components/IncomingCallOverlay.tsx": "Full-screen overlay for incoming voice calls that polls `useGetActiveCallSession` (no arguments), filters for voice-type calls, handles accept/decline with WebRTC setup, and calls `onCallActive` with the correct 3-argument signature.",
  "frontend/src/pages/ActiveCallPage.tsx": "Active call screen with live MM:SS duration timer, partner avatar, Mute/Unmute toggle (controls audio track enabled state), Speaker toggle (uses setSinkId), ICE candidate polling and application, End Call button that updates status to #ended and cleans up all media/connection resources.",
  "frontend/src/pages/VideoOutgoingCallPage.tsx": "New full-screen outgoing video call page that requests camera+mic permissions first (shows friendly denial screen if refused), displays local camera preview while waiting, creates a WebRTC offer with video tracks, polls for answer/status changes, and navigates to ActiveVideoCallPage on connection.",
  "frontend/src/components/VideoIncomingCallOverlay.tsx": "Full-screen overlay for incoming video calls that polls `useGetActiveCallSession` (no arguments), filters for video-type calls, handles camera permission denial, and calls `onCallActive` with the correct 4-argument signature including `'video'` callType.",
  "frontend/src/pages/ActiveVideoCallPage.tsx": "New active video call screen with full-screen remote video feed, draggable PiP local camera preview (mouse+touch), duration timer, mute/camera toggle, front/back camera switch via `replaceTrack`, ICE candidate polling, and end-call cleanup — all on a dark background with rose/blush accents."
}